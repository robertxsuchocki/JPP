int main () {
  [int] x;
  x = [10];
  x[0] = 42;
  x[9] = 10;
  if (length(x) == x[9]) {
    printString("success!");
    # prints success!
  } else {
    x[10] = 0;
    # this would produce an error
  }
  return 0;
}
